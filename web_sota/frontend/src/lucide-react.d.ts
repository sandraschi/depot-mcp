// Fallback declarations for lucide-react when the installed copy ships
// without its own types (old CJS dist). Remove once lucide-react is upgraded
// to a version that bundles types — then `tsc --noEmit` still guards this.
declare module "lucide-react";
